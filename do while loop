#include <stdio.h>

int main (){
    //do while loop syntax
    do{
        //body of the loop
        //code to execute if expression is true

    //}while (expression);
    int x = 1;

    do{
        printf("count is:%i \n",x);
        x++;
    } while (x<== 10);
    }
    return 0
}